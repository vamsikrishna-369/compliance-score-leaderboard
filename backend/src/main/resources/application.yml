spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tooldb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: false
    baseline-on-migrate: true

server:
  port: 8080